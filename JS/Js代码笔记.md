**Js****代码笔记**

 

**Js代码输出语句**

**alert(“”) //控制浏览器弹出警告框**

**document.write(“”) //在页面输出一个内容**

**console.log(“”) //控制台输出一个日志,打开开发者选项才能看到控制台**

 

**js代码编写位置**

**<button> 为编写一个按钮标签 一般在<body>中**

​    **同时可以为其附加属性 <button onclik=”alert(‘’)”>点击</button> 可在内添加输出语句**

<a href=”javascript:alert(‘’);”>点击</a> //将js代码写在超链接的href属性中，这样点击超链接会执行js代码 其中双引号内可以放置其他网页用于跳转

<a href=”javascript:;”>点击</a> //该指令没有反应，需要另外配合其他指令使用

***\**\*虽然可以写在标签内部，但是他们属于结构与行为耦合，不方便维护，不推荐使用\**\**\***

 

**将js代码编写到外部js文件中，通过script标签引用，写在外部文件中可以在不同的页面中同时引用，也可以利用浏览器的缓存机制**

<script scr=”****.js”>

**Script标签有一旦引用外部文件就无法编写代码如需要则再创建一个script标签用于编写内部代码**

 

**多行注释 /\* \*/**

**单行注释 //**

 

**1、** **Js中严格区分大小写**

**2、** **每条指令以分号结尾（如果不写分号系统会自动加分号，但是会消耗浏览器的性能）**

**3、** **Js中会忽略空格和换行，所以可以利用空格和换行进行美化**

 

**字面量，都是不可改变的值，也称为常量 如：1 2 3 4………**

**字面量都是可以直接使用的**

**alert(123123123); //但是不推荐直接使用**

**可以通过变量来保存字面量，变量更加方便使用**

**通过var关键字来声明一个变量
 var a;**

**a=123;**

**也可以声明与赋值同时进行**

**var a=123;**

**可以通过变量对自变量进行描述**

**var age=80;**

 

**标识符 再js中所有可以由我们自主命名的都可以称为标识符**

**在命名标识符时如下规则：**

**字母  数字  下划线  $**

**但是不能以数字开头**

**标识符不能是js中的关键字和保留字**

**标识符一般采用驼峰命名法：首字母小写，每个单词开头字母大写，其余字母小写**

**所有utf-8内容都可以当作变量**

**关键字     break       do         instanceof      typeof      case**

​           **else        new        var            catch       finally**

​           **return      vold       continue       for         switch**

​           **while       default     if             throw      delete**

​           **in         try         function        this        with**

​           **debugger   false           true           null**

 

**保留字      class           enum      extends        super       const**

​           **export      import      implements     let         private**

​           **public      yield       interface       package    protected**

​           **static**

 

 

**数据类型** 

**Js中一共有六种数据类型**

​                                    **String      字符串**

​    **Number    数值**

​    **Boolean     布尔值      基本数据类型**

​    **Null       空值**

​    **Undefined   未定义**

​    **Object      对象  ----  引用数据类型**

**字符串string需要用引号引起来 单引号也可以，不能混着用**

**var str = “hello”; //字符串**

**console.log(“hello”); //如果不加引号就变成变量了**

**str = “我说:’今日风儿甚是喧嚣’”** 

**在字符串中可以使用\作为转义字符** 

​    **\”表示”**

​    **\’表示’**

​    **\n表示换行**

​    **\t表示制表符 就相当于tab**

​    **\\表示\**

**Str=”我说:\”今日风儿甚是喧嚣\””;**

 

**var str2 = “hello”;**

**str2 = “你好”;**

**console.log(“”);**

**var写一次就可以，后面会自动定义，声明的时候写即可，str2的值是最后的代码**

 

**顺序执行，执行完str2后才会执行console**

 

**在js中所有数值都是number类型，包括整数和浮点**

**typeof检查变量的类型**

**console.log(typeof str2);**

**js中可以表示数字的最大值**

**console.log(Number.Max_VALUE);** 

**还可以console.log(Number.Max_VALUE)\* console.log(Number.Max_VALUE)；**

**如果使用number超过最大值，返回一个infinity表示无穷大，类型为number**

**NaN是一个特殊的数字，表示not a number**

**使用js进行浮点数运算，可能得到一个不精确的结果**

 

**Null值的时object类型**

**Undefined 声明一个变量但是没有赋值就是undefined，类型也是undefined**

 

**强制类型转换**

​    **将其他数据类型转换成string** 

​       **var a=123;**

​    **方式一 调用转换类型的toString() 只适用number Boolean string类型**

**a.toString(); //调用a的toString()方法**

**var b = a.toString();原变量不会改变，需要另外赋值**

​    

​    **方式二 调用String()函数转换成字符串**

**对于null 和 undefined将直接转换成”null” undefined直接转换成”undefined”**

​           **a = String(a);**

 

​    **将其他数据类型转换成number**

​    **与string方法一样，字符串转数字**

**如果是数字将转换成数字**

**如果是字符串将转换成null**

**如果字符串是一个空串或是全是空格，将转换成0**

**Null转换成数字为0，undefined转换成数字为NaN**

​    

​    **调用arseInt()函数转换成Number**

​    **a = “123px”;**

​    **a = parseInt(a); //结果为123，遇到除整数以外就退出**

​    **parseIoat()作用和parseing差不多，遇到除小数以外啊就退出**

 

**16进制 0x开头 8进制0开头  2进制0b开头**

**可以在parseInt()中传递一个第二个参数，用来指定数字的进制**

**parseInt(a,10);**

 

 

**转换成布尔值，使用Boolean()函数**

**var a = 123;**

**a = Boolean(a); /\*对于数字来说除了0和NaN其余的都是true**

​             **对于字符串来说转布尔值除了空字符串都是true**

​             **对于null和undefined来说都是false**

**对象也会转换成true\*/**

 

**运算符也叫操作符**

**通过运算符可以对一个或多个值进行运算并获取运算结果**

**typeof就是运算符，可以获得一个值的类型,它会将该值的类型以字符串的形式返回**

**对非number类型进行运算会先将其转换成Number类型，除了加法**

**任何值对NaN运算都是NaN**

**算数运算符** 

**+   对字符串进行运算会进行拼接**

​    **任何值和字符串做加法运算都会转换成字符串进行拼接**

   **我们可以利用任何值与字符串相加的特点来将值进行转换**

​    **var a=123;**

​    **a=a+””; //将a转换成字符串,隐式类型转换**

\-   **对两个值进行减法运算**

**任何值做- \* /都会转换成Nuamber**

***   乘法**

**/   除法**

**%   取模(取余)**

 

**一元运算符只需要一个操作数**

**+ - 分别为正号和负号进行运算**

**对于非Number值会先转换成Number再运算,可以通过+来进行隐式转换**

 

**&& ||对非布尔值的运算**

**&&运算两个都是true则返回后一个 两个都是false则返回前一个**

**||如果第一个值为true直接返回第一个，如果为false则返回第二个值**

 

**比较时如果两边全是字符串将会比较ASCII码，而且只会比较第一位，若两位相同，则会顺位到下一个进行比较，可以对英文进行排序**

**比较两个字符串数字时一定要先进行转型再比较，不然可能会得到不可预知的错误**

 

**\u四位编码输出unicode编码**

**&#十进制编码；**

 

**isNaN()可以判断是不是NaN类型**

**=== 全等 和相等类似，但是不会做类型的自动转换**

**!== 不全等 不会做类型的自动转换**

 

**获取a和b中的最大值**

**var max = a>b?a:b**

**获取a b c中的最大值**

**var max = max>c?max:c;**

 

**运算符优先级**

**.   []   new**

**()**

**++  --**

**！ ~ +(单目) -(单目) typeof void delete**

**% \*  /**

**+(****双目) -(双目)**

**<<  >>  >>>**

**< <=  > >=**

**==  !== ===**

**&**

**^**

**|**

**&&**

**||**

**?:**

**=****、+=、-=、\*=、/=、%=、<<=、>>=、>>>=、&=、^=、|=**

**‘**

**如果不知道运算符的优先级可以使用()来进行运算**

 

**{}代码块没有作用域这个概念，即使写在里面外部依然可以调用**

 

**语句分类** 

​    **-条件判断语句**

​    **-条件分支语句**

​    **-循环语句**

 

**条件判断语句**

​    **-使用条件判断语句只有条件成立才会执行，条件不成立则语句不执行**

​    **If语句**  

**-if(条件表达式)-条件表达式为true执行**

​       **-If(条件表达式){}**

​           **else(){}**

​       **-if(){}**

​           **else if(){}**

​              **else{}**

 

**prompt()可以弹出一个提示框，该提示框可以里面的文本框输入一个内容**

**用户输入的内容将会作为函数的一个返回值，可以定义一个变量来接收内容**

**var a = prompt(“”);**

**prompt函数返回值是string类型，直接再prompt前加+可以转换成number类型**

 

**switch() //需要全等,可以在每个case后跟break**

**{**   

​    **case 表达式1:**

​    **case 表达式2:**

​    **default:**

**}**

 

**Switch比较常用的方法**

**switch(true) //通过这种方法可以较为简单的进行比较**

**{**

​    **case a >= 60:**

**}**   

​                                           

**<span> </span> 标签使用方法**

**Span**

**{**

**display:inline-block**

​    **width:100px;**

**}**

 

 